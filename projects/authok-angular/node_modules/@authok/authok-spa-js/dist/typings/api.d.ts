import { TokenEndpointOptions, TokenEndpointResponse } from './global';
export declare function oauthToken(
  {
    baseUrl,
    timeout,
    audience,
    scope,
    authokClient,
    useFormData,
    ...options
  }: TokenEndpointOptions,
  worker?: Worker
): Promise<TokenEndpointResponse>;
